import HeroSection from "./components/HeroSection";

export default function Home() {
  return (
    <main>
      <HeroSection />
      {/* Ensuite tes sections (skills preview, projects preview, etc.) */}
    </main>
  );
}
